[package]
name = "lan-mouse-adapter-api"
description = "Versioned external-process adapter protocol for Lan Mouse file clipboard integration"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
repository = "https://github.com/feschber/lan-mouse"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.107"
thiserror = "2.0.0"
base64 = "0.22"
