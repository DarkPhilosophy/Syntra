[package]
name = "syntra-log"
description = "Runtime-reconfigurable, per-subsystem logging shared by every Syntra process"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
humantime = "2.1"
# `std` provides set_boxed_logger and the std::error::Error impls.
log = { workspace = true, features = ["std"] }
thiserror.workspace = true
