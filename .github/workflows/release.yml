name: "Release"
run-name: "Release - ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || github.event.inputs.name || github.ref_name }}"

on:
  push:
    branches: [ "main" ]
    tags:
      - v**
  workflow_dispatch:
    inputs:
      name:
        description: 'Development release name'
        required: false
        default: ''

env:
  CARGO_TERM_COLOR: always

jobs:
  linux-release-build:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v6
    - name: install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install libx11-dev libxtst-dev libfuse3-dev libgtk-4-dev
    - name: Release Build
      run: |
        cargo build --release
        cargo build --release -p lan-mouse-adapter-fuse -p lan-mouse-adapter-gtk-clipboard
        cp target/release/lan-mouse lan-mouse-linux-x86_64
        cp target/release/lan-mouse-adapter-fuse lan-mouse-adapter-fuse-linux-x86_64
        cp target/release/lan-mouse-adapter-gtk-clipboard lan-mouse-adapter-gtk-clipboard-linux-x86_64
        test -x lan-mouse-linux-x86_64
        test -x lan-mouse-adapter-fuse-linux-x86_64
        test -x lan-mouse-adapter-gtk-clipboard-linux-x86_64
    - name: Install cargo bundle
      run: cargo install cargo-bundle
    - name: Bundle
      run: cargo bundle --release
    - name: Upload build artifact
      uses: actions/upload-artifact@v6
      with:
        name: lan-mouse-linux-x86_64
        path: |
          lan-mouse-linux-x86_64
          lan-mouse-adapter-fuse-linux-x86_64
          lan-mouse-adapter-gtk-clipboard-linux-x86_64
          target/release/bundle/deb/lan-mouse_*.deb
          target/release/bundle/appimage/lan-mouse_*.AppImage

  linux-arm64-release-build:
    runs-on: ubuntu-22.04-arm
    steps:
    - uses: actions/checkout@v6
    - name: install dependencies
      run: sudo apt-get update && sudo apt-get install libx11-dev libxtst-dev libfuse3-dev libgtk-4-dev
    - name: Release Build
      run: |
        cargo build --release
        cargo build --release -p lan-mouse-adapter-fuse -p lan-mouse-adapter-gtk-clipboard
        cp target/release/lan-mouse lan-mouse-linux-arm64
        cp target/release/lan-mouse-adapter-fuse lan-mouse-adapter-fuse-linux-arm64
        cp target/release/lan-mouse-adapter-gtk-clipboard lan-mouse-adapter-gtk-clipboard-linux-arm64
        test -x lan-mouse-linux-arm64
        test -x lan-mouse-adapter-fuse-linux-arm64
        test -x lan-mouse-adapter-gtk-clipboard-linux-arm64
    - name: Install cargo bundle
      run: cargo install cargo-bundle
    - name: Bundle
      run: cargo bundle --release
    - name: Upload build artifact
      uses: actions/upload-artifact@v6
      with:
        name: lan-mouse-linux-arm64
        path: |
          lan-mouse-linux-arm64
          lan-mouse-adapter-fuse-linux-arm64
          lan-mouse-adapter-gtk-clipboard-linux-arm64
          target/release/bundle/deb/lan-mouse_*.deb
          target/release/bundle/appimage/lan-mouse_*.AppImage

  windows-release-build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v6
    - name: Release Build
      run: cargo build --release
    - name: Create Archive
      run: |
        mkdir lan-mouse-windows
        Copy-Item target\release\lan-mouse.exe lan-mouse-windows
        if (!(Test-Path lan-mouse-windows\lan-mouse.exe)) { throw "Missing lan-mouse.exe" }
        Compress-Archive -Path lan-mouse-windows\* -DestinationPath lan-mouse-windows-x86_64.zip
    - uses: actions/upload-artifact@v6
      with:
        name: lan-mouse-windows-x86_64
        path: lan-mouse-windows-x86_64.zip

  macos-release-build:
    runs-on: macos-15-intel
    steps:
    - uses: actions/checkout@v6
    - name: install dependencies
      run: |
        brew install --cask inkscape
        brew install imagemagick librsvg
    - name: Release Build
      run: |
        cargo build --release
        cp target/release/lan-mouse lan-mouse-macos-intel
    - name: Make icns
      run: scripts/makeicns.sh
    - name: Install cargo bundle
      run: cargo install cargo-bundle
    - name: Bundle
      run: |
        cargo bundle --release
        scripts/copy-macos-dylib.sh "target/release/bundle/osx/Lan Mouse.app/Contents/MacOS/lan-mouse"
    - name: Verify bundle
      run: |
        test -f target/icon.icns
        test -x "target/release/bundle/osx/Lan Mouse.app/Contents/MacOS/lan-mouse"
        test -f "target/release/bundle/osx/Lan Mouse.app/Contents/Resources/menubar-template.png"
    - name: Zip bundle
      run: |
        cd target/release/bundle/osx
        zip -r "lan-mouse-macos-intel.zip" "Lan Mouse.app"
    - name: Upload build artifact
      uses: actions/upload-artifact@v6
      with:
        name: lan-mouse-macos-intel
        path: target/release/bundle/osx/lan-mouse-macos-intel.zip

  macos-arm64-release-build:
    runs-on: macos-15
    steps:
    - uses: actions/checkout@v6
    - name: install dependencies
      run: |
        brew install --cask inkscape
        brew install imagemagick librsvg
    - name: Release Build
      run: |
        cargo build --release
        cp target/release/lan-mouse lan-mouse-macos-arm64
    - name: Make icns
      run: scripts/makeicns.sh
    - name: Install cargo bundle
      run: cargo install cargo-bundle
    - name: Bundle
      run: |
        cargo bundle --release
        scripts/copy-macos-dylib.sh "target/release/bundle/osx/Lan Mouse.app/Contents/MacOS/lan-mouse"
    - name: Verify bundle
      run: |
        test -f target/icon.icns
        test -x "target/release/bundle/osx/Lan Mouse.app/Contents/MacOS/lan-mouse"
        test -f "target/release/bundle/osx/Lan Mouse.app/Contents/Resources/menubar-template.png"
    - name: Zip bundle
      run: |
        cd target/release/bundle/osx
        zip -r "lan-mouse-macos-arm64.zip" "Lan Mouse.app"
    - name: Upload build artifact
      uses: actions/upload-artifact@v6
      with:
        name: lan-mouse-macos-arm64
        path: target/release/bundle/osx/lan-mouse-macos-arm64.zip

  release:
    name: "Release"
    needs: [windows-release-build, linux-release-build, linux-arm64-release-build, macos-release-build, macos-arm64-release-build]
    runs-on: "ubuntu-latest"
    steps:
    - name: Download build artifacts
      uses: actions/download-artifact@v4
    - name: Get short SHA
      id: vars
      run: echo "short_sha=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
    - name: Create Pre-Release
      if: ${{ !startsWith(github.ref, 'refs/tags/') }}
      uses: softprops/action-gh-release@v2
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        tag_name: ${{ format('{0}-{1}', github.event.inputs.name || github.ref_name, steps.vars.outputs.short_sha) }}
        name: ${{ format('{0}-{1}', github.event.inputs.name || github.ref_name, steps.vars.outputs.short_sha) }}
        prerelease: true
        generate_release_notes: true
        files: |
          lan-mouse-linux-x86_64/lan-mouse-linux-x86_64
          lan-mouse-linux-x86_64/target/release/bundle/deb/lan-mouse_*.deb
          lan-mouse-linux-x86_64/target/release/bundle/appimage/lan-mouse_*.AppImage
          lan-mouse-linux-arm64/lan-mouse-linux-arm64
          lan-mouse-linux-arm64/target/release/bundle/deb/lan-mouse_*.deb
          lan-mouse-linux-arm64/target/release/bundle/appimage/lan-mouse_*.AppImage
          lan-mouse-macos-intel/lan-mouse-macos-intel.zip
          lan-mouse-macos-arm64/lan-mouse-macos-arm64.zip
          lan-mouse-windows-x86_64/lan-mouse-windows-x86_64.zip
    - name: Create Tagged Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v2
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        name: ${{ github.ref_name }}
        generate_release_notes: true
        files: |
          lan-mouse-linux-x86_64/lan-mouse-linux-x86_64
          lan-mouse-linux-x86_64/target/release/bundle/deb/lan-mouse_*.deb
          lan-mouse-linux-x86_64/target/release/bundle/appimage/lan-mouse_*.AppImage
          lan-mouse-linux-arm64/lan-mouse-linux-arm64
          lan-mouse-linux-arm64/target/release/bundle/deb/lan-mouse_*.deb
          lan-mouse-linux-arm64/target/release/bundle/appimage/lan-mouse_*.AppImage
          lan-mouse-macos-intel/lan-mouse-macos-intel.zip
          lan-mouse-macos-arm64/lan-mouse-macos-arm64.zip
          lan-mouse-windows-x86_64/lan-mouse-windows-x86_64.zip
